# Production environment variables.
# Empty VITE_API_URL means the app uses relative URLs (e.g. /api/auth/login).
# vitae-ui's nginx (src-ui/nginx.conf) then proxies /api/* to vitae-api on the
# pangolin network. This keeps the same image portable across any prod hostname.
VITE_API_URL=
