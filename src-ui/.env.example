# Override the API base URL only if you want to bypass the Vite dev proxy.
# By default the proxy forwards /api to http://localhost:8000 (when running
# `npm run dev` on the host) or to http://api-dev:8000 (in docker compose),
# so this can stay unset for local dev.
# VITE_API_URL=http://localhost:8000
