# Clippy configuration for Rust linting
# This file configures Clippy lints for the Tauri backend

# Warn on common mistakes
cognitive-complexity-threshold = 30
too-many-arguments-threshold = 7
type-complexity-threshold = 250

# Deny certain lints to enforce best practices
# (These will be configured in the Rust code itself via attributes)
